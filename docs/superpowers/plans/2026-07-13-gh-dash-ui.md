# gh-dash 風 UI 刷新 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TUI の見た目を gh-dash 風に刷新する(外枠ボーダー廃止・カラム色分け・相対時刻・fetch 中インジケータ)。

**Architecture:** 変更はほぼ `crates/ghbox/src/ui.rs` の描画層に閉じる。`ghbox-core` は `Theme` に色フィールド3つを追加するのみ。fetch 中表示は main.rs の `AtomicBool` を描画直前に読み `ui::draw` の引数で渡す(App に状態を複製しない)。ロジック(fetch / filter / store)には触れない。

**Tech Stack:** Rust (edition 2024), ratatui 0.30, serde, `time` crate(RFC3339 パース用に新規追加)。

Spec: `docs/superpowers/specs/2026-07-13-gh-dash-ui-design.md`

## Global Constraints

- config は `#[serde(default, deny_unknown_fields)]`。`Theme` への追加は default があるので後方互換。
- lib のエラーは thiserror、バイナリは anyhow(本計画でエラー型追加はなし)。
- コミットメッセージは英語・conventional commits。
- 検証: `cargo test --workspace` / `cargo clippy --workspace -- -D warnings` / `cargo fmt --all`。
- コミット対象は本作業で編集したファイルのみを明示 `git add`。

---

### Task 1: `Theme` にカラム色フィールドを追加

`pr_number` / `author` / `time` の3フィールドを `Theme` に追加する(デフォルト: Green / Magenta / DarkGray)。

**Files:**
- Modify: `crates/ghbox-core/src/config.rs`(`Theme` struct 183-208行付近 / `mod tests`)

**Interfaces:**
- Produces: `Theme::pr_number: ThemeColor`, `Theme::author: ThemeColor`, `Theme::time: ThemeColor`(Task 4 の ui.rs が参照)。

- [ ] **Step 1: 失敗するテストを書く**

`crates/ghbox-core/src/config.rs` の `mod tests` 内(既存の theme テストの近く)に追加:

```rust
    #[test]
    fn theme_column_colors_default_and_override() {
        let cfg = parse("").unwrap();
        assert_eq!(cfg.theme.pr_number, ThemeColor::Named(NamedColor::Green));
        assert_eq!(cfg.theme.author, ThemeColor::Named(NamedColor::Magenta));
        assert_eq!(cfg.theme.time, ThemeColor::Named(NamedColor::DarkGray));
        let cfg = parse("[theme]\npr_number = \"cyan\"\n").unwrap();
        assert_eq!(cfg.theme.pr_number, ThemeColor::Named(NamedColor::Cyan));
    }
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test -p ghbox-core theme_column_colors_default_and_override`
Expected: コンパイルエラー(`pr_number` フィールドが存在しない)で FAIL。

- [ ] **Step 3: `Theme` にフィールドを追加**

`Theme` struct を更新:

```rust
#[derive(Debug, Clone, Deserialize)]
#[serde(default, deny_unknown_fields)]
pub struct Theme {
    pub tab_active: ThemeColor,
    pub tab_inactive: ThemeColor,
    pub border: ThemeColor,
    pub selection_bg: ThemeColor,
    pub selection_fg: ThemeColor,
    pub table_header: ThemeColor,
    pub status_bar: ThemeColor,
    pub pr_number: ThemeColor,
    pub author: ThemeColor,
    pub time: ThemeColor,
}
```

`impl Default for Theme` に追加:

```rust
impl Default for Theme {
    fn default() -> Self {
        use NamedColor::*;
        Self {
            tab_active: ThemeColor::Named(Yellow),
            tab_inactive: ThemeColor::Named(DarkGray),
            border: ThemeColor::Named(DarkGray),
            selection_bg: ThemeColor::Named(Blue),
            selection_fg: ThemeColor::Named(White),
            table_header: ThemeColor::Named(Cyan),
            status_bar: ThemeColor::Named(DarkGray),
            pr_number: ThemeColor::Named(Green),
            author: ThemeColor::Named(Magenta),
            time: ThemeColor::Named(DarkGray),
        }
    }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test -p ghbox-core && cargo build --workspace`
Expected: 全テスト PASS、ビルド成功。

- [ ] **Step 5: コミット**

```bash
git add crates/ghbox-core/src/config.rs
git commit -m "feat(core): add pr_number/author/time theme colors"
```

---

### Task 2: 相対時刻表示(`fmt_relative`)

`fmt_ts`("07-12 10:30")を相対表記(`now` / `Xm` / `Xh` / `Xd` / `MM-DD`)に置き換える。RFC3339 パースのため `time` crate を ghbox に追加。テスト可能なように now(epoch 秒)は引数注入。

**Files:**
- Modify: `crates/ghbox/Cargo.toml`(`time` 依存追加)
- Modify: `crates/ghbox/src/ui.rs`(`fmt_ts` → `fmt_relative` / `cell_text` / `column_constraint` / `draw_table` / tests)

**Interfaces:**
- Produces: `fn fmt_relative(ts: &str, now_epoch: i64) -> String`、`fn now_epoch() -> i64`(ui.rs 内部関数。Task 4 の `draw_table` も継続使用)。
- Consumes: なし(Task 1 と独立)。

- [ ] **Step 1: `time` 依存を追加**

`crates/ghbox/Cargo.toml` の `[dependencies]` に追加(アルファベット順で `tokio` の前):

```toml
time = { version = "0.3", features = ["parsing"] }
```

- [ ] **Step 2: 失敗するテストを書く**

`crates/ghbox/src/ui.rs` の `mod tests` 内で、既存の `fmt_ts_formats_iso8601` テストを削除し、代わりに追加:

```rust
    fn epoch(ts: &str) -> i64 {
        time::OffsetDateTime::parse(ts, &time::format_description::well_known::Rfc3339)
            .unwrap()
            .unix_timestamp()
    }

    #[test]
    fn fmt_relative_boundaries() {
        let now = epoch("2026-07-13T12:00:00Z");
        assert_eq!(fmt_relative("2026-07-13T11:59:30Z", now), "now");
        assert_eq!(fmt_relative("2026-07-13T11:30:00Z", now), "30m");
        assert_eq!(fmt_relative("2026-07-13T07:00:00Z", now), "5h");
        assert_eq!(fmt_relative("2026-07-11T12:00:00Z", now), "2d");
        assert_eq!(fmt_relative("2026-05-01T00:00:00Z", now), "05-01");
        // クロックスキューで未来になったタイムスタンプは "now" 扱い
        assert_eq!(fmt_relative("2026-07-14T00:00:00Z", now), "now");
        assert_eq!(fmt_relative("garbage", now), "garbage");
    }
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `cargo test -p ghbox fmt_relative_boundaries`
Expected: コンパイルエラー(`fmt_relative` 未定義)で FAIL。

- [ ] **Step 4: `fmt_relative` を実装し `fmt_ts` を置き換える**

`crates/ghbox/src/ui.rs` の `fmt_ts` 関数(79-87行)を以下で置き換え:

```rust
/// "2026-07-12T10:30:00Z" → "2d" (relative to now_epoch, unix seconds).
/// Falls back to "MM-DD" past 30 days and to the raw string when unparsable.
fn fmt_relative(ts: &str, now_epoch: i64) -> String {
    let parsed =
        time::OffsetDateTime::parse(ts, &time::format_description::well_known::Rfc3339);
    let Ok(t) = parsed else {
        return ts.to_string();
    };
    let delta = now_epoch - t.unix_timestamp();
    if delta < 60 {
        "now".to_string()
    } else if delta < 3600 {
        format!("{}m", delta / 60)
    } else if delta < 86_400 {
        format!("{}h", delta / 3600)
    } else if delta < 30 * 86_400 {
        format!("{}d", delta / 86_400)
    } else {
        ts[5..10].to_string() // RFC3339パース済みなのでASCII保証
    }
}

fn now_epoch() -> i64 {
    std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_secs() as i64)
        .unwrap_or(0)
}
```

`cell_text` のシグネチャと時刻 arm を更新:

```rust
fn cell_text(item: &Item, col: Column, now_epoch: i64) -> String {
    match col {
        Column::Repo => item.repo.clone(),
        Column::Number => format!("#{}", item.pr_number),
        Column::Title => item.pr_title.clone(),
        Column::Author => item.display_author().to_string(),
        Column::Comment => item
            .comment
            .as_ref()
            .and_then(|c| c.body.lines().next())
            .unwrap_or_default()
            .to_string(),
        Column::Updated => fmt_relative(&item.pr_updated_at, now_epoch),
        Column::Created => fmt_relative(
            match &item.comment {
                Some(c) => &c.created_at,
                None => &item.pr_created_at,
            },
            now_epoch,
        ),
    }
}
```

`column_constraint` の時刻カラム幅を相対表記に合わせて縮める(ヘッダ "UPDATED" = 7文字):

```rust
        Column::Updated | Column::Created => Constraint::Length(7),
```

`draw_table` の rows 構築部を更新:

```rust
    let now = now_epoch();
    let rows = items
        .iter()
        .map(|item| Row::new(columns.iter().map(|&c| Cell::from(cell_text(item, c, now)))));
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cargo test -p ghbox`
Expected: 全テスト PASS(既存レンダリングテストは時刻セルの文字列を assert していないため影響なし)。

- [ ] **Step 6: コミット**

```bash
git add crates/ghbox/Cargo.toml crates/ghbox/src/ui.rs Cargo.lock
git commit -m "feat(tui): show relative timestamps in time columns"
```

---

### Task 3: タブ行の手組み化と罫線行の追加

`Tabs` ウィジェットをやめて `Line` を手組みし(アクティブ = accent+bold、カウント = dim、区切り `│`)、タブ直下に水平罫線1本を足す。レイアウトを4段(タブ/罫線/テーブル/ステータス)にする。

**Files:**
- Modify: `crates/ghbox/src/ui.rs`(`draw` / `draw_tabs` / import / tests)

**Interfaces:**
- Produces: `fn draw_rule(frame: &mut Frame, theme: &Theme, area: Rect)`(ui.rs 内部)。
- Consumes: なし。

- [ ] **Step 1: 失敗するテストを書く**

`crates/ghbox/src/ui.rs` の既存テスト `renders_tabs_table_and_status` に assert を2つ追加:

```rust
        assert!(
            text.contains("マージ依頼 1 │ レビュー依頼 0"),
            "hand-built tab line with │ divider"
        );
        assert!(text.contains("─────"), "horizontal rule under tabs");
```

(既存の `assert!(text.contains("マージ依頼 1"), ...)` と `assert!(text.contains("レビュー依頼 0"), ...)` はこの2行で包含されるので削除してよい)

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test -p ghbox renders_tabs_table_and_status`
Expected: FAIL(罫線 `─────` がまだ存在しない。※タブの `│` divider assert は `Tabs` のデフォルト divider も `│` のため現状でも通る可能性があるが、罫線 assert が確実に落ちる)。

- [ ] **Step 3: レイアウトとタブ描画を実装**

`draw` の constraints を4段に変更し、罫線描画を挟む:

```rust
pub fn draw(frame: &mut Frame, app: &App, config: &Config) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(1),
            Constraint::Length(1),
            Constraint::Fill(1),
            Constraint::Length(1),
        ])
        .split(frame.area());

    draw_tabs(frame, app, &config.theme, chunks[0]);
    draw_rule(frame, &config.theme, chunks[1]);
    draw_table(frame, app, config, chunks[2]);
    draw_status_bar(frame, app, config, chunks[3]);
}
```

`draw_tabs` を手組みに置き換え:

```rust
fn draw_tabs(frame: &mut Frame, app: &App, theme: &Theme, area: Rect) {
    let dim = Style::default().fg(color(theme.tab_inactive));
    let mut spans = vec![Span::raw(" ")];
    for (i, s) in app.sections.iter().enumerate() {
        if i > 0 {
            spans.push(Span::styled(" │ ", dim));
        }
        let title_style = if i == app.active {
            Style::default()
                .fg(color(theme.tab_active))
                .add_modifier(Modifier::BOLD)
        } else {
            dim
        };
        spans.push(Span::styled(s.title.clone(), title_style));
        spans.push(Span::styled(format!(" {}", s.items.len()), dim));
    }
    frame.render_widget(Paragraph::new(Line::from(spans)), area);
}

fn draw_rule(frame: &mut Frame, theme: &Theme, area: Rect) {
    let rule = "─".repeat(area.width as usize);
    frame.render_widget(
        Paragraph::new(rule).style(Style::default().fg(color(theme.border))),
        area,
    );
}
```

import から `Tabs` を外し、`Span` を足す:

```rust
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, Borders, Cell, Paragraph, Row, Table, TableState};
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test -p ghbox && cargo clippy --workspace -- -D warnings`
Expected: 全テスト PASS、clippy 警告なし。

- [ ] **Step 5: コミット**

```bash
git add crates/ghbox/src/ui.rs
git commit -m "feat(tui): hand-built tab line with rule, drop Tabs widget"
```

---

### Task 4: テーブル刷新(ボーダー廃止・大文字ヘッダ・色分け・選択マーカー・no items)

テーブルの全周ボーダーを廃止し、ヘッダを大文字化、カラムごとに色を付け、選択行に `▌` マーカー(accent 色)を出す。空セクションは中央に `no items`(dim)。

**Files:**
- Modify: `crates/ghbox/src/ui.rs`(`column_label` / `draw_table` / import / tests)

**Interfaces:**
- Consumes: `theme.pr_number` / `theme.author` / `theme.time`(Task 1)、`cell_text(item, col, now)` / `now_epoch()`(Task 2)。
- Produces: `fn cell_style(col: Column, theme: &Theme) -> Style`(ui.rs 内部)。

- [ ] **Step 1: 失敗するテストを書く**

`renders_tabs_table_and_status` に assert を追加:

```rust
        assert!(text.contains("REPO"), "uppercase header");
        assert!(text.contains("TITLE"), "uppercase header");
        assert!(text.contains("▌"), "selection marker");
        assert!(!text.contains("┌"), "no outer border");
```

`mod tests` に空セクション用テストを追加:

```rust
    #[test]
    fn empty_section_shows_no_items_placeholder() {
        let config = Config::default();
        let titles = config.sections.iter().map(|s| s.title.clone()).collect();
        let app = App::new(titles);
        let mut terminal = Terminal::new(TestBackend::new(60, 10)).unwrap();
        terminal.draw(|f| draw(f, &app, &config)).unwrap();
        let text = buffer_text(&terminal);
        assert!(text.contains("no items"), "placeholder for empty section");
        assert!(!text.contains("REPO"), "header hidden when empty");
    }
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test -p ghbox`
Expected: 追加 assert で FAIL(ヘッダが小文字 / マーカーなし / ボーダーあり / no items 未実装)。

- [ ] **Step 3: 実装**

`column_label` を大文字に:

```rust
fn column_label(col: Column) -> &'static str {
    match col {
        Column::Repo => "REPO",
        Column::Number => "#",
        Column::Title => "TITLE",
        Column::Author => "AUTHOR",
        Column::Comment => "COMMENT",
        Column::Updated => "UPDATED",
        Column::Created => "CREATED",
    }
}
```

`cell_style` を追加(`cell_text` の直後):

```rust
/// repo/comment are fixed DarkGray context columns; number/author/time are
/// themeable so users can match their terminal palette.
fn cell_style(col: Column, theme: &Theme) -> Style {
    match col {
        Column::Repo | Column::Comment => Style::default().fg(Color::DarkGray),
        Column::Number => Style::default().fg(color(theme.pr_number)),
        Column::Title => Style::default(),
        Column::Author => Style::default().fg(color(theme.author)),
        Column::Updated | Column::Created => Style::default().fg(color(theme.time)),
    }
}
```

`draw_table` を置き換え:

```rust
fn draw_table(frame: &mut Frame, app: &App, config: &Config, area: Rect) {
    let theme = &config.theme;
    let columns = &config.sections[app.active].columns;
    let items = &app.active_section().items;

    if items.is_empty() {
        if area.height == 0 {
            return;
        }
        let centered = Rect {
            x: area.x,
            y: area.y + area.height / 2,
            width: area.width,
            height: 1,
        };
        frame.render_widget(
            Paragraph::new("no items")
                .style(Style::default().fg(Color::DarkGray))
                .alignment(Alignment::Center),
            centered,
        );
        return;
    }

    let now = now_epoch();
    let header = Row::new(columns.iter().map(|&c| Cell::from(column_label(c)))).style(
        Style::default()
            .fg(color(theme.table_header))
            .add_modifier(Modifier::BOLD),
    );
    let rows = items.iter().map(|item| {
        Row::new(
            columns
                .iter()
                .map(|&c| Cell::from(cell_text(item, c, now)).style(cell_style(c, theme))),
        )
    });
    let widths: Vec<Constraint> = columns
        .iter()
        .map(|&c| column_constraint(c, items))
        .collect();

    let table = Table::new(rows, widths)
        .header(header)
        .row_highlight_style(
            Style::default()
                .bg(color(theme.selection_bg))
                .fg(color(theme.selection_fg))
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol(Text::styled(
            "▌ ",
            Style::default().fg(color(theme.tab_active)),
        ))
        .highlight_spacing(HighlightSpacing::Always);

    let mut state = TableState::default();
    state.select(Some(app.selected));
    frame.render_stateful_widget(table, area, &mut state);
}
```

import を更新(`Block` / `Borders` を外し、`Alignment` / `Text` / `HighlightSpacing` を足す):

```rust
use ratatui::layout::{Alignment, Constraint, Direction, Layout, Rect};
use ratatui::text::{Line, Span, Text};
use ratatui::widgets::{Cell, HighlightSpacing, Paragraph, Row, Table, TableState};
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test -p ghbox && cargo clippy --workspace -- -D warnings`
Expected: 全テスト PASS、clippy 警告なし。

- [ ] **Step 5: コミット**

```bash
git add crates/ghbox/src/ui.rs
git commit -m "feat(tui): borderless table with column colors and selection marker"
```

---

### Task 5: ステータスバー刷新と fetch 中インジケータ

`draw` に `fetching: bool` を追加し、ステータスバーを「アイコン(⟳/✓) + status + dim な英語ヘルプ」にする。main.rs 側は AtomicBool を描画直前に読んで渡し、定期 fetch 開始時にも再描画させ、fetch 完了通知の前にフラグを下ろす。`now_hms` から " UTC" を、`compose_status` から "updated " を落とす(アイコンと重複するため)。

**Files:**
- Modify: `crates/ghbox/src/ui.rs`(`draw` / `draw_status_bar` / `help_line` / tests)
- Modify: `crates/ghbox/src/main.rs`(`spawn_fetch` / poll ループ / draw 呼び出し2箇所 / `now_hms` / `compose_status` / tests)

**Interfaces:**
- Produces: `pub fn draw(frame: &mut Frame, app: &App, config: &Config, fetching: bool)`(main.rs が呼ぶ唯一の公開シグネチャ変更)。
- Consumes: `fetching: Arc<AtomicBool>`(main.rs 既存)。

- [ ] **Step 1: 失敗するテストを書く**

`crates/ghbox/src/ui.rs` の `help_line_uses_primary_keys_with_arrow_glyphs` を英語表記に更新:

```rust
    #[test]
    fn help_line_uses_primary_keys_with_arrow_glyphs() {
        let kb = Keybindings::default();
        assert_eq!(
            help_line(&kb),
            "↓↑ move · ←→ section · o open · d done · r refresh · q quit"
        );
    }
```

`renders_tabs_table_and_status` の draw 呼び出しとステータスバー assert を更新
(`app.status` の設定行も新フォーマットに合わせる):

```rust
        app.status = "12:34:56".into();
        let mut terminal = Terminal::new(TestBackend::new(100, 12)).unwrap();
        terminal.draw(|f| draw(f, &app, &config, false)).unwrap();
        let text = buffer_text(&terminal);
```

```rust
        assert!(text.contains("✓ 12:34:56"), "status with idle icon");
        assert!(text.contains("q quit"), "english help from keybindings");
```

(旧 assert `updated 12:34:56 UTC` と `q:終了` は削除)

`empty_section_shows_no_items_placeholder` の draw 呼び出しも `draw(f, &app, &config, false)` に更新。

fetch 中アイコンのテストを追加:

```rust
    #[test]
    fn status_bar_shows_spinner_while_fetching() {
        let config = Config::default();
        let titles = config.sections.iter().map(|s| s.title.clone()).collect();
        let mut app = App::new(titles);
        app.status = "refreshing...".into();
        let mut terminal = Terminal::new(TestBackend::new(80, 8)).unwrap();
        terminal.draw(|f| draw(f, &app, &config, true)).unwrap();
        let text = buffer_text(&terminal);
        assert!(text.contains("⟳ refreshing..."), "fetching icon + status");
    }
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cargo test -p ghbox`
Expected: コンパイルエラー(`draw` は3引数)で FAIL。

- [ ] **Step 3: ui.rs を実装**

`draw` のシグネチャ変更と `draw_status_bar` への伝搬:

```rust
pub fn draw(frame: &mut Frame, app: &App, config: &Config, fetching: bool) {
```

```rust
    draw_status_bar(frame, app, config, fetching, chunks[3]);
```

`help_line` を英語に:

```rust
fn help_line(kb: &Keybindings) -> String {
    format!(
        "{}{} move · {}{} section · {} open · {} done · {} refresh · {} quit",
        key_glyph(kb.down.primary()),
        key_glyph(kb.up.primary()),
        key_glyph(kb.prev_section.primary()),
        key_glyph(kb.next_section.primary()),
        key_glyph(kb.open.primary()),
        key_glyph(kb.done.primary()),
        key_glyph(kb.refresh.primary()),
        key_glyph(kb.quit.primary()),
    )
}
```

`draw_status_bar` を置き換え:

```rust
fn draw_status_bar(frame: &mut Frame, app: &App, config: &Config, fetching: bool, area: Rect) {
    let theme = &config.theme;
    let icon = if fetching { "⟳" } else { "✓" };
    let line = Line::from(vec![
        Span::raw(" "),
        Span::styled(icon, Style::default().fg(color(theme.tab_active))),
        Span::raw(format!(" {}", app.status)),
        Span::styled(
            format!(" · {}", help_line(&config.keybindings)),
            Style::default().fg(color(theme.status_bar)),
        ),
    ]);
    frame.render_widget(Paragraph::new(line), area);
}
```

- [ ] **Step 4: main.rs を実装**

`now_hms` から " UTC" を落とす(doc コメントも更新):

```rust
/// HH:MM:SS UTC without pulling in chrono (shown next to the ✓ icon).
fn now_hms() -> String {
    let secs = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_secs())
        .unwrap_or(0);
    let (h, m, s) = ((secs / 3600) % 24, (secs / 60) % 60, secs % 60);
    format!("{h:02}:{m:02}:{s:02}")
}
```

`compose_status` の base から "updated " を落とす(✓ アイコンが更新済みを表すため):

```rust
    let base = match filter_error {
        Some(e) => format!("filter error: {e}"),
        None => hms,
    };
```

`spawn_fetch` の closure で、完了通知の前にフラグを下ろす(UI が結果を描画する時点で ⟳ が消えているように):

```rust
    tokio::task::spawn_blocking(move || {
        let result = handle.block_on(fetch_and_build(&token, &sections, &db_path));
        // Clear the in-flight flag before notifying the UI so the ⟳ icon is
        // already off when the result is drawn. Unwind still drops the guard.
        drop(guard);
        let _ = tx.send(Msg::Sections(Box::new(result)));
    });
```

(既存の `let _guard = guard;` 行は削除)

poll ループの `spawn_fetch` 呼び出しを、開始時に再描画が走るよう変更(キー起点の refresh はキーイベント自体で再描画されるが、定期 fetch はメッセージがないと ⟳ が出ない):

```rust
            if spawn_fetch(
                &fetch_tx,
                &fetch_fetching,
                fetch_token.clone(),
                fetch_sections_cfg.clone(),
                fetch_db_path.clone(),
            ) {
                let _ = fetch_tx.send(Msg::Redraw);
            }
```

draw 呼び出し2箇所(初回とループ内)を更新:

```rust
    terminal.draw(|f| ui::draw(f, &app, &config, fetching.load(Ordering::SeqCst)))?;
```

main.rs の `mod tests` の compose_status テスト4件を新フォーマットに更新
(引数 `"00:00:00 UTC".into()` → `"00:00:00".into()`、期待値から `updated ` と ` UTC` を除去):

```rust
    #[test]
    fn status_is_plain_update_when_no_errors() {
        let s = compose_status("00:00:00".into(), None, &[]);
        assert_eq!(s, "00:00:00");
    }

    #[test]
    fn status_leads_with_filter_error() {
        let s = compose_status("00:00:00".into(), Some("sec: exited with 1".into()), &[]);
        assert_eq!(s, "filter error: sec: exited with 1");
    }

    #[test]
    fn status_appends_api_warnings_after_update() {
        let s = compose_status(
            "00:00:00".into(),
            None,
            &["SAML enforcement".to_string(), "rate limited".to_string()],
        );
        assert_eq!(s, "00:00:00 ⚠ API: SAML enforcement; rate limited");
    }

    #[test]
    fn status_shows_both_filter_error_and_api_warnings() {
        let s = compose_status(
            "00:00:00".into(),
            Some("sec: exited with 1".into()),
            &["SAML enforcement".to_string()],
        );
        assert_eq!(s, "filter error: sec: exited with 1 ⚠ API: SAML enforcement");
    }
```

- [ ] **Step 5: 全チェックが通ることを確認**

Run: `cargo test --workspace && cargo clippy --workspace -- -D warnings && cargo fmt --all --check`
Expected: 全テスト PASS、clippy 警告なし、fmt 差分なし(差分が出たら `cargo fmt --all` 後に再確認)。

- [ ] **Step 6: コミット**

```bash
git add crates/ghbox/src/ui.rs crates/ghbox/src/main.rs
git commit -m "feat(tui): status bar with fetch indicator and english help"
```

---

### Task 6: ドキュメント更新

README の `[theme]` 例に新キーを追記する。

**Files:**
- Modify: `README.md`(`[theme]` ブロック、現行 63-65 行付近)

- [ ] **Step 1: README の theme 例を更新**

現行:

```toml
[theme]                             # 省略キーはデフォルト。ratatui 名前付き色(小文字) or "#rrggbb"
tab_active = "yellow"
selection_bg = "blue"
```

を以下に置き換え:

```toml
[theme]                             # 省略キーはデフォルト。ratatui 名前付き色(小文字) or "#rrggbb"
tab_active = "yellow"               # アクティブタブ・選択マーカー・fetch アイコンの accent 色
selection_bg = "blue"
pr_number = "green"                 # PR番号カラム。ほかに author / time カラムも指定可
```

- [ ] **Step 2: コミット**

```bash
git add README.md
git commit -m "docs: document new theme color keys"
```

---

## 完了時

`superpowers:finishing-a-development-branch` で main への統合方法を確認する。
